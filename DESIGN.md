# 🎨 Umriq — وصف التصميم الحالي

> ملف توثيقي شامل للنظام البصري الحالي لتطبيق **Umriq** (منصة مقاعد العمرة)
> آخر تحديث: يونيو 2026

---

## 1. الهوية البصرية العامة

**الطابع:** سينمائي فاخر — Obsidian (أسود ليلي) + Royal Gold (ذهبي ملكي) + لمسات Sapphire/Emerald.
**المزاج:** ليلي، عميق، راقٍ، يميل إلى Vision Pro / Apple Luxury / Stripe Dark.
**اتجاه العرض:** Mobile-first (max-width: `xl` / 36rem) — يعمل RTL كاملًا.
**الوضع:** Dark-only (لا يوجد Light Mode حاليًا).

---

## 2. نظام الألوان (OKLCH)

| الدور | المتغير | القيمة | الوصف |
|------|---------|--------|--------|
| الخلفية الأساسية | `--background` | `oklch(0.115 0.018 265)` | أسود مزرق عميق (Obsidian) |
| النص الأساسي | `--foreground` | `oklch(0.97 0.006 80)` | أبيض دافئ |
| السطح | `--surface` | `oklch(0.16 0.022 265)` | طبقة فوق الخلفية |
| البطاقات | `--card` | `oklch(0.175 0.023 265)` | لبطاقات المحتوى |
| **اللون الأساسي (Primary)** | `--primary` / `--gold` | `oklch(0.81 0.135 82)` | **ذهبي ملكي** — هوية البراند |
| الذهبي الفاتح | `--gold-soft` | `oklch(0.9 0.085 88)` | للهايلايتات |
| الذهبي العميق | `--gold-deep` | `oklch(0.62 0.135 60)` | للظلال والعمق |
| Emerald | `--emerald` | `oklch(0.74 0.16 162)` | النجاح / online |
| Crimson | `--crimson` | `oklch(0.66 0.22 25)` | التنبيهات / الإشعارات |
| Sapphire | `--sapphire` | `oklch(0.62 0.22 265)` | الـ accent الثانوي |
| Border | `--border` | `oklch(1 0 0 / 0.07)` | حد شفاف ناعم جدًا |

### التدرجات (Gradients)
- **`--gradient-gold`** — تدرج ذهبي ثلاثي (فاتح → متوسط → عميق)
- **`--gradient-gold-shimmer`** — ذهبي متحرك (لـ `text-shimmer`)
- **`--gradient-midnight`** — Radial من قمة الشاشة (تأثير هالة)
- **`--gradient-aurora`** — 3 طبقات Radial (ذهبي + ياقوتي + زمردي)
- **`--gradient-mesh`** — Conic gradient ملوّن دوّار

---

## 3. الطباعة (Typography)

| الخط | الاستخدام |
|------|-----------|
| **Tajawal** (300–900) | النصوص العربية — افتراضي عند `[dir="rtl"]` |
| **Inter** (300–900) | النصوص اللاتينية — افتراضي عند `[dir="ltr"]` |
| **Space Grotesk** (500–700) | العناوين الكبيرة (`font-display`) — مع `letter-spacing: -0.02em` |

### مقاسات شائعة
- العناوين الكبيرة: `text-[1.35rem] font-bold` مع `font-display`
- النصوص الأساسية: `text-sm` / `text-[13px]`
- التسميات الصغيرة: `text-[10px] – text-[11px]` (شريط التنقل، الـ badges)

---

## 4. الزجاجية (Glassmorphism)

ثلاث طبقات زجاج معرّفة كـ utilities في `styles.css`:

### `.glass`
زجاج خفيف عام — `blur(20px) saturate(160%)` — للعناصر الثانوية.

### `.glass-strong`
**الأكثر استخدامًا** — `blur(28px) saturate(180%)` بخلفية متدرجة عمودية من `oklch(0.22 ... / 0.78)` إلى `oklch(0.14 ... / 0.85)`. تُستخدم في:
- الـ Bottom Navigation
- زر الإشعارات العائم
- بطاقات السوق
- Install Prompt

### `.glass-gold`
زجاج بلمسة ذهبية — حد ذهبي شفاف `0.18`، خلفية ذهبية شفافة `0.08`.

---

## 5. البطاقات (Card System)

### `.card-luxe`
- خلفية: `--gradient-luxury` (تدرج داكن قطري)
- ظل: `--shadow-card`
- حد علوي مضيء عبر `::before` (linear gradient من الأبيض الشفاف للأعلى)

### `.card-gold-edge`
- يضيف حدًا ذهبيًا متدرجًا (135deg) باستخدام `::before` + `mask-composite`
- يُستخدم على العناصر المميزة (Install Prompt، البطاقات الفاخرة)

---

## 6. الظلال (Shadows)

| الاسم | الاستخدام |
|------|-----------|
| `--shadow-luxe` | ظل عميق سينمائي (24px + 8px طبقتين) — للعناصر الطافية الرئيسية |
| `--shadow-gold` | ظل ذهبي متوهج — للأزرار الذهبية والـ FAB |
| `--shadow-card` | ظل ناعم للبطاقات + inset highlight |
| `--shadow-glow` | حلقة ذهبية + توهج خارجي — للحالة Active/Focus |
| `--shadow-inner-glow` | توهج داخلي ذهبي خفيف |

---

## 7. الأنيميشن والحركة

### تأثيرات مُعرّفة (CSS)
- **`.shimmer`** — ضوء متحرك أفقي (2.4s linear)
- **`.text-shimmer`** — نص ذهبي متموج (4s)
- **`.pulse-gold`** — نبضة ذهبية حول الأزرار (2s)
- **`.aurora-bg`** — خلفية ثابتة مع كرتين ضبابيتين متحركتين (`float-1` 18s, `float-2` 22s) — ذهبية + بنفسجية
- **`.glow-orb`** — كرة دوّارة بـ conic-gradient (20s)
- **`.rise`** — ظهور مع translateY (0.7s)
- **`.grain`** — طبقة نقاط دقيقة لإضفاء ملمس فيلمي

### Framer Motion
- انتقالات الصفحات: `opacity + translateY(12) + blur(6px)` → 0 خلال 0.5s مع easing `[0.22, 1, 0.36, 1]`
- تأثيرات `whileTap: 0.9` و `whileHover: 1.05` على العناصر التفاعلية
- `layoutId="navdot"` للـ active indicator في BottomNav (spring 400/30)

---

## 8. التخطيط (Layout)

### `AppShell` (الهيكل الرئيسي)
```
┌────────────────────────────┐
│  aurora-bg (fixed, z:-1)  │  ← خلفية متحركة دائمة
├────────────────────────────┤
│  TopBar (sticky, glass)    │  ← شعار + نقطة online
├────────────────────────────┤
│  main (max-w-xl, animated) │  ← المحتوى
├────────────────────────────┤
│  Floating Notif (FAB)      │  ← bottom-28, end-5
│  BottomNav (floating glass)│  ← rounded-[32px]
└────────────────────────────┘
```

### TopBar
- Sticky علوي شفاف بـ `backdrop-blur-2xl` على خلفية `oklch(0.105 ... / 0.6)`
- يعرض الشعار أو عنوان الصفحة (بـ `font-display`)
- نقطة Emerald صغيرة متوهجة كمؤشر "online"
- **لا يحتوي** على أيقونات لغة أو إشعارات (تم نقلها)

### BottomNav (الطافي)
- مستطيل عائم بزوايا `rounded-[32px]`
- خلفية: تدرج عمودي مع `blur(36px) saturate(200%)`
- خط ذهبي شفاف فوق الحافة العلوية
- 5 عناصر: Home / Market / **Publish (FAB)** / Messages / Profile
- زر النشر الأوسط: مرفوع `-mt-8`، حجم `58px`، ذهبي مع `pulse-gold` + `shimmer` + ring ذهبي
- المؤشر النشط: شريط ذهبي صغير فوق الأيقونة (مع `layoutId`)

### زر الإشعارات العائم (FAB)
- منفصل عن BottomNav — يطفو فوقه يمينًا
- دائرة `glass-strong` بحجم 48px
- نقطة Crimson متوهجة مع `pulse-gold` كمؤشر إشعار جديد

### Install Prompt
- يظهر بعد 1.2s من حدث `beforeinstallprompt`
- بطاقة `glass-strong + card-gold-edge` بعرض كامل تقريبًا
- زر تثبيت ذهبي + زر إغلاق ✕

---

## 9. أنصاف الأقطار (Radius)

| القيمة | الاستخدام |
|--------|-----------|
| `--radius-sm` 0.5rem | عناصر صغيرة (badges) |
| `--radius-md` 0.75rem | الأزرار |
| `--radius-lg` 1rem | الحقول، الأيقونات |
| `--radius-xl` 1.25rem | البطاقات الصغيرة |
| `--radius-2xl` 1.75rem | البطاقات الرئيسية |
| `--radius-3xl` 2.25rem | الـ BottomNav (`rounded-[32px]`) |

---

## 10. تفاصيل الصفحات

### `index.tsx` (Splash)
- خلفية نجوم متحركة + كرات توهج دوّارة (`glow-orb`)
- شعار كبير + نص `text-shimmer` ذهبي
- زر دخول ذهبي مع `pulse-gold`

### `dashboard.tsx` (لوحة التحكم)
- "Live Command Center" بطابع سينمائي
- بطاقات إحصائية متوهجة + رسوم بيانية شريطية متحركة
- Hero section بعنوان كبير ذهبي

### `market.tsx` (السوق)
- بطاقات العروض بـ `glass-strong + card-gold-edge`
- مسار الرحلة: مدينتان + أيقونة طائرة وسطى ذهبية + خطوط متدرجة
- شارات الخصم (`-20%`) وشارات الحالة العاجلة
- شرائح فلترة (Filter chips) ذهبية أنيقة

### `profile.tsx` (الحساب)
- مفتاح اللغة `ar/en` كـ Segmented Control ذهبي ضمن الإعدادات
- اختصار للإشعارات في قائمة الإعدادات

---

## 11. RTL & i18n
- التطبيق ثنائي اللغة (ar/en) مع `dir` ديناميكي على `<html>`
- جميع المسافات تستخدم `start/end` بدل `left/right`
- الخط يتبدل تلقائيًا حسب الاتجاه

---

## 12. الشعور النهائي (Vibe)

> 🌌 **"تطبيق فاخر في علبة مخملية ذهبية"**
> ليلي، هادئ، متوهج بدفء ذهبي، مع لمسات سينمائية (أورورا + شيمر + بلور عميق).
> كل تفاعل مصحوب بحركة دقيقة، وكل بطاقة تبدو كأنها مرفوعة عن الخلفية بطبقة زجاج وضوء.

---

## ⚠️ ملاحظتك الحالية
لم يعجبك هذا التصميم وتريد **إعادة بناء كامل من الصفر**. هذا الملف هو خط الأساس الذي ستنطلق منه التغييرات الجديدة، حتى لا نكرر نفس القرارات البصرية.
